apiVersion: v1
kind: Service
metadata:
  name: doctor-patient-portal-service
  namespace: doctor-patient-portal
  labels:
    app: doctor-patient-portal
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: doctor-patient-portal
